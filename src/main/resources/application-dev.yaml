server:
  port:

spring:
  application:
    name: ScrumFlow
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    baseline-on-migrate: true
    enabled: true
    user:
    password:

api:
  security:
    token_secret: